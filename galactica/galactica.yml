name: galactica

channels:
  - default

dependencies:
  - python
  - ipykernel
  - scikit-learn
  - scipy
  - pip
  - pip:
    - transformers==4.28.0
    - datasets
    - accelerate
    - evaluate
    - google-cloud-storage
